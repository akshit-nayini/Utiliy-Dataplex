# Python dependencies for the metrics runner (run_metrics.py).
# Install into a venv:  python -m pip install -r requirements.txt
#
# NOT installable via pip (must already be on the host machine):
#   * Java JDK 17        (set JAVA_HOME) — required by PySpark's JVM
#   * MSSQL ODBC Driver  (18 / 17 / 13) — https://aka.ms/odbc
# The JDBC jar (mssql-jdbc) and Windows auth DLL + Hadoop winutils are vendored
# under master/drivers/ and master/hadoop/ — no install needed for those.

# Pinned to the versions this engine was built and verified against
# (Python 3.13, JDK 17).
pyspark==4.1.2
pyodbc==5.3.0

# Test dependency (optional — only for `pytest tests/`):
pytest>=8.0

# GCP + Airflow additions for validation pipeline
# (no google-cloud-datacatalog: catalog registration uses
# dataplex_v1.CatalogServiceClient - see pipeline/catalog.py - since Data
# Catalog's write API is deprecated and blocked on newer projects)
apache-airflow[gcp]==2.5.1
google-cloud-storage==2.10.0
google-cloud-bigquery==3.11.0
# 1.9.0 predates the Catalog API (CatalogServiceClient/EntryGroup/EntryType/
# AspectType) - that landed in the 2.x line, so this must be >= 2.x. Note:
# apache-airflow[gcp]==2.5.1's provider packages may cap this lower for a
# Composer install - if so, upgrade the Composer environment's Airflow
# version or install this package outside the constraints file.
google-cloud-dataplex>=2.20.0
PyYAML==6.0

# Dataflow pipeline
apache-beam[gcp]==2.52.0
